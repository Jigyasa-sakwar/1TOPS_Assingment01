`timescale 1ns / 1ps
s
module hot_bin_encoder2 #(
  parameter N = 8,
  parameter Q = $clog2(N))(

input wire [N-1:0]in,
output reg [Q-1:0]out);

integer i;
always @(*)begin
out =0;

for (i=0;i < N;i=i+1)begin
  if(in[i])
     out = i;
  end 
end

endmodule